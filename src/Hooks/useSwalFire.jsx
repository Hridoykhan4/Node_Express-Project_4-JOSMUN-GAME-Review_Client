/* import Swal from "sweetalert2";

const useSwalFire = (isSuccess, title, text) => {
  if (isSuccess) {
    return Swal.fire({
      title: title,
      text: text,
      icon: "success",
      background: "#1e1e2f",
      color: "#fff",
      confirmButtonColor: "#ff4c29",
      confirmButtonText: "Go On 🎮",
    }).then((result) => {
      if (result.isConfirmed) {
        nav(state ? state : "/");
      }
    });
  }
};

export default useSwalFire;
 */